name: flame_texture_atlas_editor
description: Editor to create Flame texture atlas

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flame:
    path: ../../flame

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
